name: HealthChecks System CI

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - src/Pulse.System/**
      - test/Pulse.System.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_system_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
    tags-ignore:
      - release-*
      - preview-*

  pull_request:
    branches: [ master ]
    paths:
      - src/Pulse.System/**
      - test/Pulse.System.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_system_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets

jobs:
  build:
    uses: ./.github/workflows/reusable_ci_workflow.yml
    with:
      PROJECT_PATH: ./src/Pulse.System/Pulse.System.csproj
      TEST_PROJECT_PATH: ./test/Pulse.System.Tests/Pulse.System.Tests.csproj
      CODECOV_FLAGS: System
