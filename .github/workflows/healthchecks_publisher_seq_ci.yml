name: HealthChecks Publisher.Seq CI

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - src/Pulse.Publisher.Seq/**
      - test/Pulse.Publisher.Seq.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_publisher_seq_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
    tags-ignore:
      - release-*
      - preview-*

  pull_request:
    branches: [ master ]
    paths:
      - src/Pulse.Publisher.Seq/**
      - test/Pulse.Publisher.Seq.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_publisher_seq_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets

jobs:
  build:
    uses: ./.github/workflows/reusable_ci_workflow.yml
    with:
      PROJECT_PATH: ./src/Pulse.Publisher.Seq/Pulse.Publisher.Seq.csproj
      TEST_PROJECT_PATH: ./test/Pulse.Publisher.Seq.Tests/Pulse.Publisher.Seq.Tests.csproj
      CODECOV_FLAGS: Publisher.Seq
