name: HealthChecks Publisher.Datadog CI

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - src/Pulse.Publisher.Datadog/**
      - test/Pulse.Publisher.Datadog.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_publisher_datadog_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets
    tags-ignore:
      - release-*
      - preview-*

  pull_request:
    branches: [ main ]
    paths:
      - src/Pulse.Publisher.Datadog/**
      - test/Pulse.Publisher.Datadog.Tests/**
      - test/_SHARED/**
      - .github/workflows/healthchecks_publisher_datadog_ci.yml
      - .github/workflows/reusable_ci_workflow.yml
      - Directory.Build.props
      - Directory.Build.targets

jobs:
  build:
    uses: ./.github/workflows/reusable_ci_workflow.yml
    with:
      PROJECT_PATH: ./src/Pulse.Publisher.Datadog/Pulse.Publisher.Datadog.csproj
      TEST_PROJECT_PATH: ./test/Pulse.Publisher.Datadog.Tests/Pulse.Publisher.Datadog.Tests.csproj
      CODECOV_FLAGS: Publisher.Datadog
